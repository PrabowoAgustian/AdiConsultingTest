<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.dirumahajanews">

    <application
        android:name=".DiRumahAjaApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:largeHeap="true"
        android:theme="@style/AppTheme.noActionBar"
        android:usesCleartextTraffic="true"
        tools:replace="android:allowBackup">
        <activity
            android:name=".feature.view.splash.SplashScreenActivity"
            android:screenOrientation="portrait"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".feature.view.splash.AfterSplashScreenActivity"
            android:screenOrientation="portrait" />
        <activity android:name=".feature.view.main.HomeActivity"
            android:screenOrientation="portrait"/>
        <activity android:name=".feature.view.detailpage.DetailNewsActivity"
            android:screenOrientation="portrait"/>
        <activity android:name=".feature.view.searchpage.SearchNewsActivity"
            android:screenOrientation="portrait"/>
    </application>

</manifest>